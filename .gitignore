# Test tooling only — the site itself stays dependency-free, and everything
# else at repo root is deployed as-is.
node_modules/
tests/test-results/
tests/playwright-report/

# Build output — regenerate with `npm run build`, never edit or commit.
build/
